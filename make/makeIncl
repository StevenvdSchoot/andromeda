CC=gcc
LD=ld
MAKE=make
OUTC=compressed
OUTD=core
COMPRESSED=-D\ __COMPRESSED
MAKEEND=FLAGS="$(FLAGS) " DEFS="$(DEFS)" $(DEFS) DIR="$(DIR)" INCLUDE="$(DIR)/make/makeIncl $(DIR)/make/x86"
test=$(foreach T,$($(1)),$(if $($(firstword $(subst :, ,$T))),$(lastword $(subst :, ,$T)),))
